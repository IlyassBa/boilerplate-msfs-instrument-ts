import { describe, it, expect } from 'vitest';
import { AltimeterPage } from './AltimeterPage';

describe('AltimeterPage', () => {
  it('should render', () => {
    expect(AltimeterPage).toBeDefined();
  });
});